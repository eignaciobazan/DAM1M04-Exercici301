<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    {{> header}}
    <title>Principal</title>
</head>
<body>
    <section class="texto">
    <h1>{{site.title}}</h1>
    <h1>{{site.subtitle}}</h1>
    </section>
    <ul>
        {{#each cities.cities}}
            {{#if coastal}}
                <span class="badge coastal">🌊 Costanera</span>
                {{else}}
                <span class="badge inland">Interior</span>
            {{/if}}

            

            <li>
                <strong>{{name}}</strong> - {{lookup ../countries.countries country}}:
                {{#if population}}
                    {{population}} habitants
                    {{else}}
                    <em>Población desconocida</em>
                {{/if}}
            </li>
        {{/each}}
    </ul>
</body>
</html>